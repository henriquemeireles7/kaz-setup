{
  "name": "my-app",
  "version": "0.0.1",
  "type": "module",
  "main": "platform/server/app.ts",
  "scripts": {
    "dev": "bun --hot platform/server/app.ts",
    "build": "bun build platform/server/app.ts --outdir dist --target bun --minify",
    "start": "bun run dist/app.js",
    "check": "bunx biome ci . && bunx tsc --noEmit && bun run harden && bun test",
    "harden": "bun platform/scripts/harden-check.ts",
    "test": "bun test",
    "lint": "bunx biome check --write --unsafe .",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bun platform/scripts/migrate.ts",
    "db:studio": "bunx drizzle-kit studio"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.5.0",
    "@t3-oss/env-core": "^0.12.0",
    "better-auth": "^1.2.0",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.7.0",
    "postgres": "^3.4.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.11",
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0"
  }
}
